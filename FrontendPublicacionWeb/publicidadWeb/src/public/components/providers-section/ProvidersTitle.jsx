// src/components/providers-section/ProvidersTitle.jsx
export default function ProvidersTitle({ title }) {
  return (
    <h2 id="providers-title" className="providers-title">
      {title}
    </h2>
  );
}
